//declare public class and launch function
public class insertionSort {
    public static void main(String[] args) {
    }
}

//declare array of integers

int [] arrayInt = {bunch of integers :)};

// apply insertion sort algorithm, code representing the outer loop for going through each element in the array

for i=1 to n-1 {
    int current = A[1];
    j = 1;
}

// while loop for insertion and comparison between i and j in order to sort and place elements of the array

while (j > 0) and A[j-1] > current {
    swap (A[j-1]), A[j]);
    j = j -1;
}

//code to print or display the sorted insertion sort of integers from the array

system.out.println("Sorted array" - etc)

