import java.util.LinkedList;

public class singlylinkedlist { 

    public static void main(String args[])
    {
        public LinkedList { //default constructor, constructs empty list
            myList myList = new myList();

            return myList;
        
        LinkedList myList = new LinkedList(A);  

        myList.add("B");
        myList.add("C");
        myList.add("D");
        myList.addFirst("Z");

        System.out.println();

        myList.remove("C");
        myList.removeFirst();
        myList.removeLast();

        //accessing first element
        System.out.println("First element is: " + myList.removeFirst());

        //accessing last element
        System.out.println("Last element is : " + myList.removeLast());
        }
    }
}